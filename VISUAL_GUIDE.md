# 🎨 GOOGLE SHEETS DATABASE - VISUAL GUIDE

## 📐 KIẾN TRÚC HỆ THỐNG

```
┌─────────────────────────────────────────────────────────────┐
│                    GOOGLE SHEETS                             │
│  ┌──────────────┬──────────────┬──────────────┬───────────┐ │
│  │   Sections   │    Tables    │    Images    │ Metadata  │ │
│  │              │              │              │           │ │
│  │ element_id   │ table_id     │ element_id   │element_id │ │
│  │ content      │ row_number   │ content (URL)│ content   │ │
│  │ type         │ column_1..n  │ type         │ type      │ │
│  └──────────────┴──────────────┴──────────────┴───────────┘ │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ Published as Web (JSON)
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  GOOGLE SHEETS API                          │
│                  (docs.google.com)                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ Fetch JSON
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    JAVASCRIPT CODE                          │
│                    (index.html)                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  GoogleSheetsAPI.fetchSheet()                          │ │
│  │         ↓                                               │ │
│  │  Parse JSON → Array of Objects                         │ │
│  │         ↓                                               │ │
│  │  CacheManager.set() → LocalStorage (5 min)            │ │
│  │         ↓                                               │ │
│  │  ContentUpdater.updateElement()                        │ │
│  └────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ Update DOM
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    WEBSITE (index.html)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  <h3 id="section-onboard">1. ONBOARD</h3>             │ │
│  │  <img id="image-123" src="...">                        │ │
│  │  <table id="table-456">...</table>                     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [🔄 Refresh Content Button]                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 LUỒNG DỮ LIỆU

```
┌─────────────┐
│   Editor    │ ───── Chỉnh sửa nội dung
└──────┬──────┘
       │
       ↓
┌─────────────────────┐
│  Google Sheets      │
│  ┌─────────────────┐│
│  │ Sections        ││ ← Tab 1
│  ├─────────────────┤│
│  │ Tables          ││ ← Tab 2
│  ├─────────────────┤│
│  │ Images          ││ ← Tab 3
│  ├─────────────────┤│
│  │ Metadata        ││ ← Tab 4
│  └─────────────────┘│
└──────────┬──────────┘
           │
           │ Publish to Web
           ↓
    ┌──────────────┐
    │ Public URL   │ ← JSON endpoint
    └──────┬───────┘
           │
           │ Fetch
           ↓
    ┌──────────────┐
    │   Website    │
    │              │
    │ [Refresh]  ← User clicks
    └──────┬───────┘
           │
           │ Update DOM
           ↓
    ┌──────────────┐
    │ New Content  │ ✅ Displayed
    └──────────────┘
```

---

## 🗂️ CẤU TRÚC FILE

```
tai-lieu-phan-phoi/
│
├── 📄 index.html                    ← Main file (có Google Sheets code)
│
├── 📁 templates/                    ← CSV Templates
│   ├── Sections.csv                 ← Template tab Sections
│   ├── Tables.csv                   ← Template tab Tables
│   ├── Images.csv                   ← Template tab Images
│   └── Metadata.csv                 ← Template tab Metadata
│
├── 📘 GOOGLE_SHEETS_SETUP.md        ← Hướng dẫn chi tiết (36 sections)
├── 📗 README_DATABASE.md            ← Quick start guide
├── 📙 TEMPLATE_GUIDE.md             ← Template instructions
├── 📕 INSTALLATION_SUMMARY.md       ← Summary & checklist
├── 📔 VISUAL_GUIDE.md               ← File này
│
└── 🔧 test-console.js               ← Test commands
```

---

## 🎯 MAPPING: SHEET → WEBSITE

### Example 1: Update Text
```
GOOGLE SHEETS (Sections tab):
┌────────────────────┬─────────────────┬──────┐
│ element_id         │ content         │ type │
├────────────────────┼─────────────────┼──────┤
│ section-onboard    │ 1. ONBOARD MỚI │ text │
└────────────────────┴─────────────────┴──────┘
                          ↓
WEBSITE:
<h3 id="section-onboard">1. ONBOARD MỚI</h3>
                         ^^^^^^^^^^^^^^^
                         Tự động cập nhật
```

### Example 2: Update Image
```
GOOGLE SHEETS (Images tab):
┌────────────────────┬─────────────────────────────┬──────┐
│ element_id         │ content                     │ type │
├────────────────────┼─────────────────────────────┼──────┤
│ image-logo         │ https://imgur.com/new.jpg  │ src  │
└────────────────────┴─────────────────────────────┴──────┘
                          ↓
WEBSITE:
<img id="image-logo" src="https://imgur.com/new.jpg">
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^
                         Tự động cập nhật
```

---

## 🔑 KEY CONCEPTS

### 1. Element ID
```
HTML:
<div id="my-element">Old content</div>
       ^^^^^^^^^^^^
       Đây là element_id

GOOGLE SHEETS:
element_id = "my-element"
```

### 2. Content Type
```
type = "text"  → Update textContent
type = "html"  → Update innerHTML
type = "src"   → Update src attribute (images)
type = "href"  → Update href attribute (links)
```

### 3. Cache
```
Lần 1:     Fetch từ Google Sheets     ← Chậm (network)
           ↓
           Save to LocalStorage
           
Lần 2-N:   Load từ LocalStorage       ← Nhanh (local)
           (trong 5 phút)
           
Sau 5 min: Fetch lại từ Google Sheets
```

---

## 📊 DATA FLOW DIAGRAM

```
┌──────────────────────────────────────────────────────────┐
│                    USER ACTIONS                          │
└────────────────────┬─────────────────────────────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
    ↓                ↓                ↓
┌─────────┐    ┌─────────┐    ┌─────────┐
│  Edit   │    │  Click  │    │ Console │
│ Sheets  │    │ Refresh │    │ Command │
└────┬────┘    └────┬────┘    └────┬────┘
     │              │              │
     │              ↓              │
     │         ┌─────────────┐    │
     │         │   Fetch     │    │
     │         │   Sheets    │ ←──┘
     │         └──────┬──────┘
     │                │
     │                ↓
     │         ┌─────────────┐
     └────────→│   Update    │
               │   Content   │
               └──────┬──────┘
                      │
                      ↓
               ┌─────────────┐
               │   Display   │
               │   Changes   │
               └─────────────┘
```

---

## 🎨 UI ELEMENTS

### Refresh Button
```
┌─────────────────────────────┐
│  Position: Fixed            │
│  Bottom-right: 20px         │
│  Z-index: 9999             │
│                             │
│  ┌───────────────────────┐  │
│  │  🔄 Refresh Content   │  │
│  │  Green gradient       │  │
│  │  Hover: Lift up       │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### Notifications
```
┌─────────────────────────────┐
│  Position: Fixed            │
│  Top-right: 20px            │
│  Auto-hide: 3 seconds       │
│                             │
│  ┌───────────────────────┐  │
│  │ ✅ Success            │  │ ← Green
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ ⚠️ Error              │  │ ← Red
│  └───────────────────────┘  │
└─────────────────────────────┘
```

---

## 🔍 DEBUG WORKFLOW

```
Problem: Content not updating
         ↓
    ┌────────────────┐
    │ Open Console   │
    │ (F12)          │
    └────────┬───────┘
             │
             ↓
    ┌────────────────┐
    │ Check Errors   │
    └────────┬───────┘
             │
    ┌────────┴────────┐
    │                 │
    ↓                 ↓
┌─────────┐    ┌──────────────┐
│ Network │    │   Console    │
│ Tab     │    │   Logs       │
└────┬────┘    └──────┬───────┘
     │                │
     ↓                ↓
┌─────────────────────────────┐
│  Check:                     │
│  • Sheet ID correct?        │
│  • Published?               │
│  • Public access?           │
│  • element_id match?        │
└─────────────────────────────┘
```

---

## 📝 SHEET STRUCTURE

### Tab: Sections
```
Row 1 (Headers):
┌────────────┬─────────┬──────┐
│ element_id │ content │ type │
└────────────┴─────────┴──────┘

Row 2+ (Data):
┌──────────────────┬──────────────┬──────┐
│ section-onboard  │ 1. ONBOARD   │ text │
├──────────────────┼──────────────┼──────┤
│ section-hub      │ 5. HUB       │ text │
└──────────────────┴──────────────┴──────┘
```

### Tab: Images
```
Row 1 (Headers):
┌────────────┬─────────────────────┬──────┐
│ element_id │ content             │ type │
└────────────┴─────────────────────┴──────┘

Row 2+ (Data):
┌────────────┬─────────────────────┬──────┐
│ image-1    │ https://imgur.../jpg│ src  │
├────────────┼─────────────────────┼──────┤
│ logo       │ https://cdn.../png  │ src  │
└────────────┴─────────────────────┴──────┘
```

---

## 🚀 QUICK COMMANDS

### Console Commands
```javascript
// 1. Refresh all content
SheetDB.refresh()

// 2. Clear cache
SheetDB.clearCache()

// 3. Update single element
SheetDB.updateElement('element-id', 'New text', 'text')

// 4. View config
console.log(SheetDB.config)

// 5. Find all IDs
document.querySelectorAll('[id]').forEach(el => {
    console.log(el.id)
})
```

---

## 🎓 LEARNING PATH

```
Day 1: Setup
├── Read GOOGLE_SHEETS_SETUP.md
├── Create Google Sheet
├── Import templates
└── Publish sheet

Day 2: Configure
├── Get Sheet ID
├── Update index.html
└── Test refresh

Day 3: Customize
├── Add more elements
├── Update content
└── Test updates

Day 4+: Advanced
├── Custom types
├── Batch updates
└── Automation
```

---

## 📈 PERFORMANCE

```
Without Cache:
Request → Google API → Parse → Update
   ↓         ↓          ↓        ↓
 10ms      500ms      50ms     10ms
         SLOW (570ms total)

With Cache:
Request → LocalStorage → Update
   ↓            ↓           ↓
 10ms         5ms         10ms
         FAST (25ms total)
```

---

## ✅ CHECKLIST

### Setup
- [ ] Google Sheet created
- [ ] 4 tabs added (Sections, Tables, Images, Metadata)
- [ ] Templates imported
- [ ] Sheet published
- [ ] Sheet ID copied

### Configuration
- [ ] index.html updated with Sheet ID
- [ ] Tested in browser
- [ ] Refresh button visible
- [ ] Console shows success

### Testing
- [ ] Edit content in sheet
- [ ] Click refresh button
- [ ] Content updates
- [ ] Cache works (5 min)

---

## 🎉 SUCCESS INDICATORS

```
✅ Console shows: "Content loaded successfully!"
✅ Refresh button appears bottom-right
✅ Green notification on refresh
✅ Content updates when clicked
✅ No errors in Console
```

---

**Visual Guide Complete! 🎨**
**Refer to other docs for detailed steps**
